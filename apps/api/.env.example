# Database
DATABASE_URL="postgresql://user:password@localhost:5432/faaif_db?schema=public"

# Email (SMTP)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="noreply@faaif.group"
SMTP_PASS="your-smtp-password"
EMAIL_FROM="FAAIF Group <noreply@faaif.group>"
EMAIL_IR="ir@faaif.group"

# CORS
CORS_ORIGIN="https://faaif.group"

# App
PORT="4000"
NODE_ENV="development"
